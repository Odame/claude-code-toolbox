# Optional, unimplemented. Ships pure Python via `uv run` for now (see README).
# Future: compile plain-english-checker to a standalone binary with PyInstaller/Nuitka.
compile-plain-english-checker:
	@echo "Not implemented. plain-english-checker currently ships as pure Python via 'uv run'." && exit 1
